#!/usr/bin/env ruby

# Furnace - Tournament runner wrapper
# Just delegates to parallel_tournament.rb which has all the logic

script_path = File.join(File.dirname(__FILE__), "../scripts/parallel_tournament.rb")
exec("ruby", script_path, *ARGV)